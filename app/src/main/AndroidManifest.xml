<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mikeporet.autoremind">

    <application
        android:allowBackup="true"
        android:icon="@drawable/circle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Start_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Car_Home" />
        <activity
            android:name=".TaskInstructionsActivity"
            android:label="@string/title_activity_task_instructions"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MakesActivity"></activity>
    </application>

</manifest>